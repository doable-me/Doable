# HEAL-RUN env1 — 2026-05-10

**Task:** Task #3 HEAL-RUN — execute `setup-v3/heal-stale-project-node-modules.sh` on env1 (`54.37.128.179`) for real (no `--dry-run`).

**Owner:** doable-qa-heal (claude-qa)
**Status:** IN-PROGRESS (live)
**Server:** ubuntu@54.37.128.179 (env1)
**Script (server):** /tmp/heal.sh (scp'd from `setup-v3/heal-stale-project-node-modules.sh`)
**Log (server):** /tmp/heal.log
**Launch cmd:** `sudo nohup bash /tmp/heal.sh > /tmp/heal.log 2>&1 &`
**Launch PID:** 96174 (under sudo wrapper 96172)
**PROJECTS_ROOT:** /opt/doable/services/api/projects
**Dry run:** 0 (live install)

## Per-project install command (from script line 72)

```
timeout 240 sudo -u doable bash -c "cd $dir && NODE_ENV=development npm install --legacy-peer-deps --include=dev --loglevel=error"
```

## Logic recap

- Iterates UUID-named subdirs of PROJECTS_ROOT
- Reads each `package.json`; required tool is `vite` if `"vite"` present, else `next` if `"next"` present, else SKIP
- If `node_modules/<tool>/package.json` already exists → SKIP (idempotent)
- Otherwise runs npm install with NODE_ENV=development + --include=dev + --legacy-peer-deps
- Per-project hard timeout: 240s

## Live progress (T+90s after launch)

| Metric                 | Value |
|------------------------|-------|
| Projects attempted     | 45    |
| Healed (ok)            | 44    |
| Failed (post-install still missing or install error) | 0 |
| Process alive          | yes (PID 96174) |

Frameworks observed so far: mostly `vite`, a few `next`. No timeouts, no install errors yet.

## Next checkpoints

- T+~3min — capture 50+ healed milestone, ping team-lead
- T+~5min — interim runlog update
- Final — when PID 96174 exits, append `=== summary ===` block with TOTAL/HEALED/SKIPPED/FAILED + FAIL_LIST

(See _live-tail_ block below for end-state.)
