# Progress Snapshot — 2026-05-02 (pre-wave-3)

> Honest progress ledger before kicking off the next implementation wave.
> Ranks every PRD/phase by completion %.

## Commit ladder so far

```
a877c9c docs: STATUS update — Phase 5 + PRD 10 foundation shipped
3fa412e Phase 5 + PRD 10 wiring: systemd template + Caddy connector-proxy passthrough
deb469c Phase 5 + PRD 10 foundation: runtime adapters + connector bridge proxy
d6b2efe docs: end-of-session status (intermediate)
3a42401 fix: thread framework_id from template to project row at scaffold
354f84d Phase 3 follow-on: Next.js (App Router) blank template
19b7a61 Phase 4: cross-platform sandbox backends (Psroot/bubblewrap/sandbox-exec)
e494bb2 Phase 2 + Phase 3 minimum: framework-aware prompts + nextjs-app adapter
fd02965 Phase 1 Wave 4: PR-G + PR-H + PR-I — final 3 Phase-1 surfaces
f0d2d80 Phase 1 Wave 3 partial: PR-E + PR-F
8a98aff Phase 1 Wave 2: PR-B/C/D — first 3 surfaces on FrameworkAdapter
7f817dc Phase 1 Wave 1: registry bootstrap + golden-file harness
cc83b81 Phase 0: framework-agnostic foundation
542d294 (snapshot/pre-framework-agnostic-2026-05-02)
```

## Per-PRD completion

| PRD | % done | What's missing |
|---|---|---|
| **02** Framework abstraction | **100%** | — |
| **03** Build event protocol | 0% | not started — full PRD pending |
| **04** Redaction filter pipeline | 0% | not started — full PRD pending |
| **05** Live build UI | 0% | not started — full PRD pending |
| **06** Runtime + publish | **40%** | foundation done; pipeline integration + supervisor loop missing |
| **07** Implementation plan | **70%** | Phase 0–4 done, Phase 5 partial |
| **07a** Phase 1 ops plan | **80%** | golden-file harness shipped, capture against snapshot not run |
| **08** AI framework awareness | **60%** | dispatch wired; per-framework skill files not done |
| **09** AI awareness rollout | **30%** | system prompt change shipped; editor de-Vite + dovault config-guard pending |
| **10** Connector bridge | **50%** | endpoint + Caddy passthrough live; postMessage handshake + AI skill missing |
| **11** Cross-platform sandbox | **60%** | 3 backends shipped; psroot.exe vendoring + opt-in tier + smoke tests pending |
| **12** AI awareness implementation | **20%** | per-framework skill files + editor de-Vite + dovault config-guard pending |

## Wave 3 targets (this push, ranked by leverage)

| # | Item | Owner pattern | Est. |
|---|---|---|---|
| 1 | Phase 5 pipeline integration (DoableCloudAdapter calls runtime adapter on `process`-kind publish + addProcessRoute + INSERT project_runtime row) | direct edit (load-bearing existing code) | 15 min |
| 2 | dovault config-guard per-framework templates (replaces hardcoded Vite literals) | direct edit (existing file) | 10 min |
| 3 | Per-framework skill files: `services/api/src/ai/skills/<framework>/connector-fetch.md` for vite-react + nextjs-app | Opus agent (NEW markdown files) | 8 min |
| 4 | Nuxt FrameworkAdapter (mirrors nextjs-app.ts shape) | Opus agent (1 NEW file) | 6 min |
| 5 | SvelteKit FrameworkAdapter | Opus agent (1 NEW file) | 6 min |
| 6 | Editor postMessage JWT handshake (PRD 10) — token endpoint + iframe injection | direct edit (touches apps/web + services/api) | 20 min |
| 7 | Editor UI de-Vite-ing (6 cosmetic copy edits in apps/web/src) | Opus agent | 8 min |
| 8 | Astro adapter | Opus agent (1 NEW file) | 6 min |
| 9 | Routes for runtime status/restart/logs | direct edit | 10 min |

## Deferred to next session
- Supervisor loop (systemd journal subscription)
- PRD 03 / 04 / 05 (build event protocol, redaction, live build UI) — multi-day
- Django / FastAPI / Hono / Expo adapters
- Framework picker UI in dashboard
- psroot.exe vendoring + sandbox smoke tests
- Apple Container + gVisor opt-in backends
- Visual-edit on non-Vite frameworks (PRD 02 §10.1 open issue)

## Operational rules for Wave 3
- Opus model only.
- 4-min hard target per agent (1-min buffer below the user's 5-min ceiling).
- Pre-arm Monitor watching expected file mtimes BEFORE spawn so silent failures surface fast.
- Send status ping at T+60s + T+180s to every active agent.
- Direct fallback the moment an agent is silent past 90s with no file activity.
- Agent failures from prior waves logged in `STATUS-2026-05-02.md`; pattern is consistent enough that single-file mechanical tasks should be done directly when agent overhead exceeds task size.
