Hey, SPAWN A TEAM to do this based on the 1-14 points having this as foundation and UI agent as one of them...I need you to test the AI chat if it's working like a real user. Try creating an application. Always link it with Supabase as a backend. Supabase is already integrated and connected, so it should work. Use it like a real user, sending follow-up messages just like a real user would. As you would continue building this app, check if:
- all the updates are live
- the user interface is responding quickly
- the preview is updating quickly
- The existing supabase database is being used; that is the supabase database.
-Refresh the page and check if all the history and every element that is shown during the live generation is also shown after it's generated.
-Check if it's correctly detecting when it's generated and when it's done and if it's stopping.
-Is a user interface perfect and usable by any business or creative user? Does it just work?
Nothing should be stale, and it should not take unusually long times. It should work perfectly, because this has to be a quality product so investors are accepted and we get the investment. I'm gonna log in through OAuth with my Gmail ID, and I'm gonna keep it locked in. What I want you to do is use it like a user via the Chrome browser, but you're not going to do it alone. You are going to do it using team create with your teammates, following the steps below.

1. First inspect the app and produce a prioritized use case list.
2. ASESS each task and Then create a team of specialized Opus 4.6 or Sonnet or Haiku agents DEPENDING ON THE WORK with explicit responsibilities.
3. Run live end to end testing through Integrated Browser/Chrome on localhost with one agent acting as a real user in the UI.
4. In parallel, have other agents monitor logs, APIs, websockets, streaming, preview updates, DB changes, file changes, and raw LLM responses. One agent to look at all these, collect statistics and metrics on the process to help you make data driven decisions. Also an agent to find root cause behind the bugs by looking into the code (Opus 4.6 agent for this).
5. You should be able to see ALL the agents outputs simultaneously somehow. use a oneliner to see them all periodically and also make them ping you.
7. For every tested interaction, compare the full chain from user action to final visible output.
8. Detect and investigate any mismatch between backend reality, streamed output, preview state, generated work, and final UI.
9. Write bugs into bugs folder with root cause info,metrics, workflow with repro steps, evidence, likely cause, and severity.
10. IF WORK SO FAR IS DONE, CLOSE DOWN THE OLD TEAM. Now, once this is done, reorganize, create a new team for fixing those bugs. These must be mostly Opus 4.6 agents
11. Each bug fix needs An agent to verify the BUG , An agent to find the root cause behind the bug to confirm if what was already found is true, an agent to fix the bug.
12. You can fix multiple bugs in parallel as long as you can organize the team without overlapping each other
13. Once bugs are fixed, you need to goback to STEP 2 and regroup into testing team again to VERIFY IF ITS REALLY FIXED.
14. IF NOT FIXED, again, same thing repeats...UNTIL ITS FIXED, TESTED AND PROVED!