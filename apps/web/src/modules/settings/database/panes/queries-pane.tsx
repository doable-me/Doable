"use client";

import { SectionCard } from "@/modules/settings/components/project-settings-shared";

export function QueriesPane() {
  return (
    <SectionCard title="Queries" description="Run parameterised SELECT queries against your database.">
      <p className="text-sm text-muted-foreground">Coming soon.</p>
    </SectionCard>
  );
}
